#pragma once
#include <iostream>
#include <string>

class User
{
private:
	std::string userName, password;
	int currency;

public:
	User(std::string userName, std::string) : userName(userName), 
		password(password) {}
};
